import { HomeViewData } from "routers/handlers/generic";

const title = "Home handler for index router";
const sampleKey = "sample value for home page screen";
const backURL = "#";

export const homeDefaultViewData: HomeViewData = {
    title,
    sampleKey,
    backURL
};
